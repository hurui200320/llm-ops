# 20260411

The Q8_0 weights take up 32 GB, so they definitely will not fit in 24 GB of VRAM. That leaves 8 GB on the CPU, and this is a dense model too—I dread to think how slow it will be.

64K context:
+ Q8_0, 31 layers loaded on the GPU: initial inference speed of 4.2 t/s, dropping to 3.4 t/s after one conversation turn (one tool call).
+ Q6_K, 39 layers loaded on the GPU: initial inference speed of 5.6 t/s, dropping to 5 t/s after one conversation turn (one tool call).
+ Q5_K_M, 42 layers loaded on the GPU: initial inference speed of 7.2 t/s, dropping to 6.4 t/s after one conversation turn (one tool call).
+ Q5_K_S, 44 layers loaded on the GPU: initial inference speed of 8.6 t/s, dropping to 7.2 t/s after one conversation turn (one tool call).

With Q8 quantization, nearly half of the 61 layers must run on the CPU, making it very slow. Q6_K is only marginally better.

Changing the batch size from the default of 2048 to 512 (https://mintlify.wiki/ggml-org/llama.cpp/advanced/performance-tuning#platform-specific-tips)
showed no noticeable improvement in VRAM usage or inference speed during testing.

# 20260418

The second GPU arrived, so there is now plenty of VRAM. However, fitting the entire Q6_K model on the GPUs with little or no reduction in context size requires a smaller ubatch.
With ubatch set to 32, pp is only a little over 100 t/s, which is completely unusable for long inputs (208K context). So I had to drop down to Q5_K_M.

The current configuration achieves pp of 280 t/s and tg of 14 t/s (actual context close to 200K: the three Sayaka Saeki novels plus one round of questions and answers).

Gemma 4's KV cache optimization is still somewhat lacking. The model itself is smaller than the 26B model, but its greater number of layers makes the KV cache much larger.

# 20260419

Watch out for out-of-memory errors: a single Gemma 4 context checkpoint takes up 1 GB, and llama-server allows 32 checkpoints by default. No wonder it keeps running out of memory.

211K context test:
 kvo: pp 288.05 t/s, tg 14.26 t/s
nkvo: pp had already fallen below 140 t/s at around 57% completion, so I stopped the test. Started a new conversation and sent “Hello”: tg 9.1 t/s.

With nkvo, increasing ub is still slow.
Using `ctk f16 ctv q8_0` requires a custom build; otherwise, computation can only run on the CPU.
Setting both to `q8_0` allows GPU computation without recompiling, but affects output quality. Also, non-f16 KV caches seem to cause memory leaks on ROCm.

# 20260920

15t/s tg is really slow for benchmarking. Try adding MTP seems to double the generation speed.
Rerun speed bench is required.

No MTP:  pp 355.04 t/s  tg 14.66 t/s
Has MTP: pp 315.22 t/s  tg 11.24 t/s  accept_rate 0.6514
(draft token 2)

Tested at 200k+ context. However, with much shorter context, like benchmark, tg can be improved from 22 to 34 t/s (at 1k context).

Switched to DFlash (Q4), but VRAM is not enough, have to shrink ub from 256 to 128.
Result:  pp 215.92 t/s  tg 11.67 t/s accept_rate 0.5168
(draft token 2, should be 4 or 8 but I forget to update the settings)

Didn't speed up much, might be limited by VRAM.

Has MTP, but ub shrinked from 256 to 128:
Result:  pp 219.32 t/s  tg 11.20 t/s accept_rate 0.6407
(draft token 2)

This setup has 2GB VRAM left, so it should not slow down due to VRAM.
LLM said `at 220K your draft path costs more than an entire target decode step`,
it means the MTP decoding on long context cost more times than it saved.

To fit dflash model, lower from 256K context to 240K (245760).
Result:  pp 319.41 t/s  tg  9.61 t/s  accept_rate 0.3388
(draft token 4)
Result:  pp 319.16 t/s  tg 12.61 t/s  accept_rate 0.5447
(draft token 2)

Note the context is 16k shorter, so the result should be faster than the 256k anyway.
With benchmark: about 36 t/s. (no MTP is 22 t/s and has MTP is 34 t/s)


256k is good until it's too slow, so I'd say 16k less context for faster speed is fine.
The dflash one is pretty fast on short context, and is not as bad as MTP on long ctx
(-2 t/s compared to no MTP, +1 t/s compared to has MTP).

# 20260922 Drop Gemma 4 31B Q5_K_M text

Speed too slow:

+ `google-gemma-4-31b-q5km-text`, 240k
  + 20%: pp  713.32 t/s  tg 21.75 t/s  accept_rate 0.5309
  + 40%: pp  520.11 t/s  tg 17.94 t/s  accept_rate 0.5352
  + 60%: pp  406.13 t/s  tg 14.79 t/s  accept_rate 0.5341
  + 85%: pp  319.47 t/s  tg 12.48 t/s  accept_rate 0.5435

Both pp and tg slow, and we can't improve it by moving mmproj to CPU
to free up VRAM for bigger ubatch size. Downgrade the quants will be Q4,
but we already have Q4 QAT version deployed.

So we decide to drop this model.

For alternatives, try novita or deepinfra, they have BF16 or at least FP8 deployed,
it cost money, but for better experience.
> https://openrouter.ai/google/gemma-4-31b-it#providers

Deleted config:

```yaml
models:
  # MTP will be slow at longer ctx, dflash won't suffer from this. But dflash requires more VRAM.
  # Serve 240K instead of 256K context, save some VRAM for dflash
  # also cannot pin draft model to one GPU via `--spec-draft-device ROCm0` because error:
  #   pre-allocated tensor (cache_k_l58) in a buffer (ROCm1) that cannot run the operation (NONE)
  "google-gemma-4-31b-q5km-text":
    name: "Google Gemma 4 31B Q5_K_M text"
    description: "240K context, no vision"
    cmd: |
      ${docker-server-rocm}
        ${gemma4-ctx-ckpt}
        --model                 /models/gemma-4/google-31B-it/google_gemma-4-31B-it-Q5_K_M.gguf
        --model-draft           /models/gemma-4/google-31B-it-DFlash/gemma4-31b-it-dflash-Q4_K_M.gguf
        --chat-template-file /templates/gemma-4/31B-chat-template.jinja
        --spec-type draft-dflash --spec-draft-n-max 2
        --jinja
        --reasoning on
        -b 2048 -ub 256
        -ngl 99
        --main-gpu 0 --split-mode layer --tensor-split 33,28
        --ctx-size 245760
        --temp 1.0 --top-p 0.95 --top-k 64 --min-p 0.0 --repeat-penalty 1.0
        --load-mode mmap
        --no-kv-unified
        --flash-attn on
        --parallel 1
    cmdStop: docker stop ${MODEL_ID}
    proxy: http://127.0.0.1:${PORT}
    checkEndpoint: /health
    concurrencyLimit: 2
    timeouts:
      connect: 30
      keepalive: 30
      # pp may take a long time so we don't set a timeout here
      responseHeader: 0
      tlsHandshake: 10
      idleConn: 90

# reference
macros:
  # common start of docker run llama server
  "docker-server-rocm": >
    docker run --name ${MODEL_ID} --init --rm
      --device /dev/dri
      --device /dev/kfd
      --group-add=video
      --group-add=render
      --security-opt label=disable
      --security-opt seccomp=unconfined
      -p ${PORT}:8080
      -v /mnt/unenc-xfs/models:/models:z
      -v /mnt/unenc-xfs/llm-ops/templates:/templates:z
      ghcr.io/ggml-org/llama.cpp:full-rocm
      --server
      --port 8080 --host 0.0.0.0
      --alias ${MODEL_ID}
      -lv 4

  # cache and ctx-checkpoint settings for gemma 4
  # ctx-checkpoint will be huge since gemma 4 has a lot of standard attn layer
  # so we have to shrink the number of ckpts (default to 32) to reduce RAM usage, otherwise get OOM kill
  # on gemma 4 31b, each checkpoint takes 1GB of RAM
  "gemma4-ctx-ckpt": >
    --cache-ram 0
    --ctx-checkpoints 20
    --checkpoint-min-step 13107
```
